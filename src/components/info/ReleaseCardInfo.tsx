export default function ReleaseCardInfo() {
  return (
    <div className="w-full h-full space-y-4 text-justify flex flex-col items-center">
      <p>
        This card provides a snapshot of releases associated with the project.
        At the moment it's displaying dummy data but on live projects, this will filter and display the most critical releases, giving
        everyone on the project an understanding on high priority tasks.
      </p>
    </div>
  );
}
